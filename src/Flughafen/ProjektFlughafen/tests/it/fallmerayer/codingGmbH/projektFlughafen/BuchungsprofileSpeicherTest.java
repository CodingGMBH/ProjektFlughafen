package it.fallmerayer.codingGmbH.projektFlughafen;

import org.junit.After;
import org.junit.Assert;
import org.junit.Before;
import org.junit.Test;

/**
 * Created by stdeptho on 27.03.2017.
 */
public class BuchungsprofileSpeicherTest {

    //BuchungsprofileSpeicher bps = Buchungsprofilespeicher.getInstance();


    @Test
    public void savedCorrectly(){
        /*
        * Buchungsprofile before;
        * Buchungsprofil after;
        * bps.save(before); --> Achtung, wie wird gespeichert?
        * after = bps.getBuchungsprofile(id);
        * after.equals(before);

        * Test ist dann bestanden, wenn after.equals(before) true zuruckliefert und somit das Buchungsprofil sauber gespeichert wurde
        * */
    }





    @Before
    public void before(){
        //Daten von File holen
        //Stream holen
    }

    @After
    public void after(){
        //Filepointer dereferenzieren
        //Stream schliesse
    }
}
